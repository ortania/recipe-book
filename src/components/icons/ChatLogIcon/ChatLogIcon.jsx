import chatLogIconSvg from "./chat-log-icon.svg";

function ChatLogIcon({ width = 20, height = 20 }) {
  return <img src={chatLogIconSvg} width={width} height={height} alt="" />;
}

export default ChatLogIcon;
